{
  "name": "salaboy-miniverse",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n vite,server -c cyan,magenta \"vite\" \"miniverse --no-browser\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "miniverse"
  },
  "dependencies": {
    "@miniverse/core": "^0.2.0",
    "@miniverse/server": "^0.2.0"
  },
  "devDependencies": {
    "@miniverse/generate": "^0.2.0",
    "concurrently": "^9.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
