# Intent mapping using Gerkin Format

For each prompt executed against this project, create a HISTORY.md 
which logs the prompt and the features expressed using gerkin format.

New feature should be added by creating new scenarios as demonstrated in the customer.feature file.

